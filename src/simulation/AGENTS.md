# Simulation Notes

The rollout must be deterministic (fixed seed, fixed horizon/policy depth) so
figures and manuscript numbers reproduce exactly. The generative model must
match the GNN spec for the sophisticated-inference track; verify via concordance.
