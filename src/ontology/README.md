# Ontology bindings

Shared vocabulary that keeps the multi-track exemplar referring to the same entities.

- `bindings.py` — maps ontology terms (states, observations, factors) to their
  representation in each track (prose label, GNN symbol, code identifier).
