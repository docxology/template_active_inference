# Ontology Notes

The ontology is the naming contract across tracks. Add or rename a term here
once; every track reads its label from the bindings rather than hard-coding
strings, so terminology cannot drift between prose, formalism, and code.
