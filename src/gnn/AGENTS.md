# GNN Notes

The GNN specification is the canonical description of each model. When a model
changes, update the `.gnn.md` source and re-run concordance — do not let the
parser, analytical math, and simulation drift apart. Parsing must reject
malformed specifications loudly.
