# Section Fragment Notes

These fragments are the EDITABLE manuscript sources (the numbered files in
`../` are composed outputs). Keep each track's fragment consistent with the
others for that section — same model, same ontology terms, same numbers. The
sheaf manifest determines which fragments compose and in what order.
