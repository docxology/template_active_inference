The analytical figures do two jobs here. [@fig:ising_mi_curve] shows the closed-form mutual-information oracle against an independent exact recomputation, while [@fig:gnn_ontology_concordance] binds the same symbols through the GNN file and ontology map. Together they make the analytical claim both statistical and semantic: the numeric curve is reproduced, and its symbols remain traceable across manuscript, GNN, and ontology artifacts.

<!-- Registry-driven figure block: see figures.yaml section_figures.methods_analytical -->
