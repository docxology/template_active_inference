# Full sheaf-coverage section fragments

A section exercising EVERY track, so the sheaf composer is demonstrated at full
coverage.

- `prose.md`, `formalism.md`, `gnn.md`, `ontology.yaml`, `simulation.md`,
  `pymdp.md`, `lean.md`, `visualization.md`, `animation.md` — one fragment per
  track, all describing the same model.
