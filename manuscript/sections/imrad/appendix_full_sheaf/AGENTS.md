# Full-Coverage Fragment Notes

This section is the coverage canary: it includes every track, so missing/empty
fragments are caught by the sheaf coverage check (`src/manuscript/sheaf/coverage.py`).
Keep all track fragments present and mutually consistent.
