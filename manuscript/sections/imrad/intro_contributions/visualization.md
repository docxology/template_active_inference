The opening architecture figure ([@fig:multi_track_architecture]) is the visual index for the paper's main claim: analytical, pymdp, and sheaf-composition tracks are not separate appendices, but one reproducible pipeline. Its source family is the track registry plus the sheaf manifest, so the counts it displays are regenerated before composition rather than hand-entered.

<!-- Registry-driven figure block: see figures.yaml section_figures.intro_contributions -->
