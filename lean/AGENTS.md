# Lean Track Notes

The Lean statements must describe the same objects as the analytical track. Keep
the toolchain pinned (`lean-toolchain`) for reproducible builds. A statement that
is `sorry`/axiom-backed is not proven — surface that honestly rather than
claiming a closed proof. Check `#print axioms` for keystone theorems.
