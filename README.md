# template_active_inference

Release artifacts for the [`docxology/template`](https://github.com/docxology/template) `template_active_inference` exemplar.

PDFs and metadata are published via `scripts/publish_project_release.py`; do not treat this repository as manuscript source.
