# Active Inference Agent Metadata

Local agent metadata for the active-inference exemplar. The project skill lives
under `skills/template-active-inference/`; project behavior remains governed by
the root and nearest `AGENTS.md` files.
